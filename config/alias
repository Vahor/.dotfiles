#!/usr/bin/env bash


alias vim="nvim"
alias vi="nvim"
alias niff="nvim -d"

#alias python="python3"
alias pip="pip3"

alias pn="pnpm"

# Shortcuts
alias reload_profile="source ~/.zsh_profile"
alias reload_shell="exec ${SHELL} -l"
alias week="date +%V"
alias h="cd ~/dev/helosion"

alias l="ls"

alias ll="ls -l"
alias lla="ll -A"

alias cdd="cd .."
alias cddd="cd .. && cd .."

alias dot="cd ~/.dotfiles"
alias rmdsstore="find . -type f -name '*.DS_Store' -ls -delete"

alias o="open ."
alias c="code ."

# Show/hide hidden files in the Finder
alias showfiles="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"
alias hidefiles="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"

alias urlencode='node --eval "console.log(encodeURIComponent(process.argv[1]))"'

alias reveal-md="reveal-md --theme night --highlight-theme hybrid --port 1337"

# Git
alias gcm="git commit -m"
alias gs="git status"
alias go="git status"
alias gh="_glog | peco | awk '{print $2}' | xargs -I {} git diff {}^ {}"
alias _glog="git log --pretty=format:\"%Cgreen%h %Creset%cd %Cblue[%cn] %Creset%s%C(yellow)%d%C(reset)\" --graph --date=relative --decorate --all"

